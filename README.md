# Epstein Unredact CLI Tool.
# Python tool used in CLI used to take the Epstein File batches (Vol0001-0008).
# Takes OG files and removes any 'bad redactions'.
# I never firgured away to find the files that had the redactions.
# I was planning on doing a word count on ech file and the ones that had more words then # the OG file had a "bad redaction", in at least that's the plan.
Python script to process/unredact Epstein files from CLI.
